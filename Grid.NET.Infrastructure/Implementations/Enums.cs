namespace Grid.NET.Infrastructure.Implementations
{
    public static class Enums
    {
        public enum FilterType
        {
            Range,
            Exact,
            StringLike,
        } 
    }
}